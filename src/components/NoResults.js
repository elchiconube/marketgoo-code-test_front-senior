import React from "react";

const NoResults = () => (
    <h2 className="center">
        Ups! There's no player to show, try to add a new one or change the
        search text
    </h2>
);

export default NoResults;
